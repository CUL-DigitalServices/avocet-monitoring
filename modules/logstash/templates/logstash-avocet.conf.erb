input {
    tcp {
        codec   => "json"
        port    => 5505
        type    => "raw"
    }
}

output {
    elasticsearch {
        codec    => json
        host     => localhost
        protocol => http
    }
    stdout {
        codec => rubydebug
    }
}
