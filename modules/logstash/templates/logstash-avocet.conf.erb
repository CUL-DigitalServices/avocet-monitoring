input {
    syslog {
        codec   => "json"
        host    => "127.0.0.1"
        port    => 5505
        type    => "raw"
    }
}

output {
    elasticsearch {
        codec    => json
        host     => localhost
        protocol => http
    }
    stdout {
        codec => rubydebug
    }
}
